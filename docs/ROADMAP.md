# 12Frames — Пошаговый roadmap от 0 до релиза

Этот документ — подробный чек-лист с шагами от чистого VPS/репозитория до первого публичного релиза (v1.0.0).

Условные обозначения:

- ✅ — выполнено,
- 🔜 — ближайший фокус,
- ⏳ — в очереди.

---

## Этап 0. Идея и подготовка

1. [✅] Определить цель первой версии:
   - Один тип календаря: **1 месяц на листе (A4)**.
   - Минимальный список шаблонов (10–20).
   - Экспорт в PDF и PNG.
2. [✅] Утвердить основные пользовательские сценарии:
   - Быстрый календарь за 5 минут.
   - Глубокий кастомный календарь.
   - Ремикс существующего шаблона.
3. [✅] Утвердить стек (Vue 3 + Vite, NestJS, PostgreSQL, Redis, Puppeteer).
4. [✅] Создать репозиторий GitHub `12frames`.

---

## Этап 1. Базовая инфраструктура проекта

### 1.1. Структура репозитория

1. [✅] Создать базовый монорепо скелет (как в `docs/ARCHITECTURE_AND_DEPLOYMENT.md`):
   - `apps/frontend`,
   - `apps/backend`,
   - `worker/render-worker`,
   - `packages/shared`,
   - `docker/`, `docs/`.
2. [✅] Добавить `README.md` (описание продукта) — уже есть черновик.
3. [✅] Добавить `.env.example` с базовыми переменными окружения.
4. [✅] Настроить `package.json` с workspace'ами и скриптами.

### 1.2. Docker и базы

1. [✅] Поднять локально `postgres` и `redis` через `docker-compose`.
2. [✅] Проверить подключение к БД из backend'а.
3. [✅] Настроить базовую схему БД (миграции):
   - `users`,
   - `projects`,
   - `templates`,
   - `render_jobs` (опционально).

---

## Этап 2. Backend — фундамент

### 2.1. Auth и пользователи

1. [✅] Инициализировать NestJS-проект в `apps/backend`.
2. [✅] Настроить `ConfigModule`, подключение к PostgreSQL через TypeORM/Prisma.
3. [✅] Создать сущность `User` (email, пароль, роли, метаданные).
4. [✅] Реализовать:
   - `POST /auth/register`,
   - `POST /auth/login`,
   - `GET /auth/me`.
5. [✅] Сделать защиту эндпоинтов через JWT (`Authorization: Bearer`).

### 2.2. Проекты календарей

1. [✅] Создать сущность `Project` (как JSON-описание календаря).
2. [✅] Реализовать CRUD:
   - `GET /projects` — список проектов пользователя,
   - `POST /projects` — создать новый проект,
   - `GET /projects/:id`,
   - `PUT /projects/:id`,
   - `DELETE /projects/:id`.
3. [✅] Добавить поле `isPublic` для Workshop.
4. [✅] Добавить базовую валидацию DTO.

### 2.3. Шаблоны и Workshop (MVP)

1. [✅] Создать сущность `Template`:
   - ownerId (может быть системный),
   - JSON-конфиг,
   - превью-изображение,
   - теги.
2. [✅] Эндпоинты:
   - `GET /templates` — публичная галерея,
   - `GET /templates/:id`,
   - `POST /templates` — создание шаблона (для авторов),
   - `POST /templates/:id/fork` — создание проекта на основе шаблона.

---

## Этап 3. Frontend — прототип редактора

### 3.1. Каркас приложения

1. [ ] Создать Vite + Vue 3 + TS-проект в `apps/frontend`.
2. [ ] Настроить:
   - Vue Router (страницы: Home, Editor, Workshop),
   - Pinia (store для пользователя и проектов),
   - Tailwind CSS.
3. [ ] Подключить API-клиент (axios) с `VITE_API_BASE_URL`.

### 3.2. Аутентификация

1. [ ] Страницы:
   - `/auth/login`,
   - `/auth/register`.
2. [ ] Сохранение `access_token` и профиля пользователя.
3. [ ] Редиректы на `/` после логина, logout.

### 3.3. Список проектов и простой редактор

1. [ ] Страница `/`:
   - Список проектов,
   - Кнопка "Создать проект",
   - Возможность открыть проект в редакторе.
2. [ ] Страница `/editor/:projectId`:
   - Отображение "макета" календаря (простая сетка месяцев),
   - Панель настроек:
     - базовые цвета,
     - шрифт заголовка,
     - загрузка картинок (fake/mock, можно пока без реального upload).
3. [ ] Связка с backend:
   - чтение/сохранение `Project` через API.

---

## Этап 4. Рендеринг и SmartPrint™ (MVP)

### 4.1. Render Worker

1. [ ] Инициализировать `worker/render-worker` как Node-проект.
2. [ ] Подключить Bull/BullMQ и Redis.
3. [ ] Настроить очередь `render`.
4. [ ] Реализовать обработчик:
   - Принимать `projectId` и формат (`pdf` / `png`),
   - Открывать `FRONTEND_RENDER_URL/:projectId` в headless-режиме,
   - Сохранять файл в `RENDER_OUTPUT_DIR`.

### 4.2. Backend-API для экспорта

1. [ ] Эндпоинты:
   - `POST /projects/:id/render` — создать задачу рендера,
   - `GET /render-jobs/:jobId` — получить статус задачи,
   - (опционально) webhook/callback при завершении.
2. [ ] Связь задач с пользователем (`ownerId`), логгирование.

### 4.3. Frontend-интеграция

1. [ ] Кнопка `Export` в редакторе:
   - вызывает `POST /projects/:id/render`,
   - опрашивает статус,
   - после завершения показывает ссылку на скачивание.
2. [ ] Минимальный UI прогресса (спиннер / "Генерируем PDF…").

### 4.4. SmartPrint™ (базовый)

1. [ ] Реализовать пресеты форматов:
   - A4 вертикальный,
   - A3 вертикальный (задаётся через CSS и параметры Puppeteer).
2. [ ] Добавить расчёт:
   - полей,
   - безопасных зон,
   - bleed (внутренний отступ).
3. [ ] Скрывать UI-элементы при рендере (отдельный "render layout" роут).

---

## Этап 5. Workshop и публичная галерея

### 5.1. Backend

1. [ ] Эндпоинты:
   - `GET /workshop` — лента публичных проектов/шаблонов,
   - `GET /workshop/:id` — просмотр,
   - `POST /workshop/:id/fork` — форк в личный проект.
2. [ ] Фильтрация/сортировка по тегам и новизне.

### 5.2. Frontend

1. [ ] Страница `/workshop`:
   - карточки календарей,
   - фильтры по тегам,
   - пагинация.
2. [ ] Страница `/workshop/:id`:
   - превью календаря,
   - кнопка "Использовать как шаблон".

---

## Этап 6. Подготовка к публичному релизу (v1.0.0)

### 6.1. Качество и UX

1. [ ] Пройтись по основным сценариям как пользователь:
   - создать аккаунт,
   - сделать календарь с нуля,
   - использовать шаблон,
   - экспортировать и распечатать.
2. [ ] Исправить критические UX-баги.
3. [ ] Добавить onboarding-подсказки в редакторе.

### 6.2. Безопасность и стабильность

1. [ ] Включить HTTPS на прод-домене.
2. [ ] Проверить:
   - основные запросы защищены JWT,
   - нет утечки приватных файлов (`/renders` только для своих? или публичные ссылки с токеном).
3. [ ] Настроить логирование и алерты (Sentry/Logtail/Prometheus).

### 6.3. Документация

1. [ ] Обновить `README.md` (добавить скриншоты, демо-ссылку).
2. [ ] Обновить `docs/ARCHITECTURE_AND_DEPLOYMENT.md` (учесть фактические изменения).
3. [ ] Оформить `docs/CONTRIBUTING.md`:
   - правила PR,
   - код-стайл,
   - ветвление (`main`, `develop`, feature-ветки).

---

## Этап 7. Релиз v1.0.0

1. [ ] Выбрать домен (например, `12frames.app`).
2. [ ] Настроить DNS → VPS/балансировщик.
3. [ ] Обновить `.env` для прод-сервера.
4. [ ] Собрать и поднять стек в проде:

   ```bash
   git pull
   docker compose build
   docker compose up -d
   ```

5. [ ] Запустить smoke-тесты:
   - health-checkи сервисов,
   - тест авторизации,
   - тест создания/экспорта календаря.
6. [ ] Создать GitHub релиз `v1.0.0` и тег `v1.0.0`.
7. [ ] Обновить лендинг/README: "Public Beta" / "v1.0.0".

---

## После релиза

- Сбор обратной связи от первых пользователей.
- Приоритизация фич:
  - многопользовательское редактирование,
  - дополнительные форматы (poster, настенный календарь),
  - расширение Smart Typography,
  - платёжная система (подписка, платные шаблоны).

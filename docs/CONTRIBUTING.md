# 12Frames — Руководство для контрибьюторов

Спасибо, что хотите помочь развивать 12Frames! Ниже — базовые правила и процесс участия.

---

## 1. Как предложить идею или сообщить о баге

1. Зайдите во вкладку **Issues** репозитория.
2. Создайте новый Issue и выберите подходящий шаблон (Bug / Feature / Question).
3. Заполните поля максимально подробно:
   - ожидаемое поведение,
   - фактическое поведение,
   - шаги для воспроизведения,
   - окружение (браузер, ОС).

---

## 2. Как внести изменения (Pull Request)

1. Форкните репозиторий и склонируйте его к себе.
2. Создайте ветку от `main`:

   ```bash
   git checkout main
   git pull origin main
   git checkout -b feature/my-awesome-feature
   ```

3. Внесите изменения в код/документацию.
4. Убедитесь, что проект собирается и локальные тесты проходят.
5. Сделайте коммит с осмысленным сообщением:

   ```bash
   git commit -m "feat: add calendar typography presets"
   ```

6. Запушьте ветку и создайте Pull Request в основной репозиторий.
7. Опишите суть изменений в PR, ссылку на соответствующий Issue (если есть).

---

## 3. Стиль кода

> Конкретный набор инструментов (ESLint/Prettier и т.п.) будет зафиксирован позже. Общие принципы:

- Пишите понятные имена переменным и функциям.
- Разделяйте ответственность: одна функция — одна задача.
- Стремитесь к простым и читаемым решениям.

---

## 4. Frontend — рекомендации

- Vue 3 Composition API, предпочтительно `<script setup>`.
- Логика работы с API — в отдельных модулях (`src/api`).
- Состояние — в Pinia stores (`src/stores`).
- Компоненты:
  - UI-компоненты (`src/components/ui`) максимально переиспользуемые,
  - страницы (`src/pages`) без лишней логики.

---

## 5. Backend — рекомендации

- NestJS-модули по доменам (`UsersModule`, `ProjectsModule`, `RenderModule`).
- DTO и валидаторы — обязательно для публичных эндпоинтов.
- Не смешивайте бизнес-логику и контроллеры:
  - контроллеры — только транспорт (HTTP),
  - сервисы — бизнес-логика.

---

## 6. Документация

- Если ваш PR затрагивает архитектуру или процесс разработки:
  - обновите соответствующие файлы в `docs/`,
  - добавьте краткое описание изменений в PR.

---

## 7. Лицензия и права

На момент раннего прототипирования проект распространяется по схеме **All rights reserved**. Участие через PR подразумевает согласие с тем, что ваши изменения могут быть использованы и переработаны в рамках проекта 12Frames.

---

Спасибо за ваш вклад! 🙌
